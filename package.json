{
  "name": "twilio-serverless-github-actions",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy",
    "jest": "jest"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0",
    "jest": "^26.5.2"

  },
  "engines": {
    "node": "10.17.0"
  }
}
